diff --git a/src/python/epicscorelibs/config.py b/src/python/epicscorelibs/config.py
index 2693281..109e88a 100644
--- a/src/python/epicscorelibs/config.py
+++ b/src/python/epicscorelibs/config.py
@@ -7,7 +7,13 @@ from copy import deepcopy
 import platform
 import sysconfig
 
-from setuptools_dso.probe import ProbeToolchain
+# The compiler identification macros ('__GNUC__', '__clang__', '_MSC_VER')
+# are probed once, when this package is built, and baked into the generated
+# ._config module alongside the ABI-related 'cxxdefs'
+# (cf. setup.py's GenVersionError.genToolchainConfig()). Reuse that result
+# here rather than invoking a C/C++ compiler (via setuptools_dso) again on
+# every import.
+from ._config import toolchain_macros
 
 
 __all__ = (
@@ -28,7 +34,7 @@ def _makeconf():
     }[platform.system()]
 
     # select epics CMPLR_CLASS
-    ID = ProbeToolchain().eval_macros(('__GNUC__', '__clang__', '_MSC_VER'))
+    ID = toolchain_macros
     if ID['__clang__']:
         cmplrname = conf['CMPLR_CLASS'] = 'clang'
     elif ID['__GNUC__']:
